{# styles:

  hero--outline
  hero--large
  hero--bg-bottom

#}
<div class="hero {{ style | join(' ') }} "
  style="background-image: url('{{ image.url }}')">
  <div class="container">
    {% if title %}

    <h1 class="hero__title">{{title}}</h1>

    {% endif %}
  </div>
</div>
