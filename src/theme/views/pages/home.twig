{% extends 'layouts/base.twig' %}

{% block content %}

  {% set hero = page.get_field('hero') %}

  {% if hero.image  %}
    {% include 'components/hero.twig' with { 'hero': hero } %}
  {% endif %}

  <div class="container">

    {% if page %}

      {{ page.content }}

    {% endif %}

  </div>

  <quotes-swiper>
  {% include 'components/swiper.twig' with { 'slides': swiper, 'pagination': true } %}
  </quotes-swiper>

  {% include 'components/logo-bar.twig' %}

  <div class="container">

    {% include 'components/categorylist.twig' with { 'showcase': true } %}

    <h2 class="is-centered" >SLS – Blog</h2>
    {% include 'components/post-overview.twig' with { 'posts': posts } %}

  </div>

{% endblock %}
